# KC custom gems.
# Zammad natively loads Gemfile.local* files (see the bottom of the main Gemfile).
# This file is copied to the repo root by apply-overlay.sh during Docker build.
#
# Example:
#   gem 'httparty', '~> 0.21'
