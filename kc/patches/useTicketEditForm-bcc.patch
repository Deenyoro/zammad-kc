diff --git a/app/frontend/shared/entities/ticket/composables/useTicketEditForm.ts b/app/frontend/shared/entities/ticket/composables/useTicketEditForm.ts
index 888e4c7150..799df7647e 100644
--- a/app/frontend/shared/entities/ticket/composables/useTicketEditForm.ts
+++ b/app/frontend/shared/entities/ticket/composables/useTicketEditForm.ts
@@ -81,7 +81,7 @@ export const useTicketEditForm = (
     }
   })
 
-  const articleTypeFields = ['to', 'cc', 'subject', 'body', 'attachments', 'security'] as const
+  const articleTypeFields = ['to', 'cc', 'bcc', 'subject', 'body', 'attachments', 'security'] as const
 
   const articleTypeFieldProps = articleTypeFields.reduce((acc, field) => {
     acc[field] = {
@@ -149,6 +149,17 @@ export const useTicketEditForm = (
         type: 'hidden',
         name: 'shared_draft_id',
       },
+      {
+        if: '$currentArticleType.fields.bcc',
+        name: 'bcc',
+        label: __('BCC'),
+        type: 'recipient',
+        validation: articleTypeFieldProps.bcc.validation,
+        props: {
+          contact: recipientContact,
+          multiple: true,
+        },
+      },
       {
         screen: 'edit',
         object: EnumObjectManagerObjects.Ticket,
